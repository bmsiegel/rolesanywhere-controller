---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: profiles.rolesanywhere.services.k8s.aws
spec:
  group: rolesanywhere.services.k8s.aws
  names:
    kind: Profile
    listKind: ProfileList
    plural: profiles
    singular: profile
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Profile is the Schema for the Profiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProfileSpec defines the desired state of Profile.
            properties:
              acceptRoleSessionName:
                description: |-
                  Used to determine if a custom role session name will be accepted in a temporary
                  credential request.
                type: boolean
              durationSeconds:
                description: |-
                  Used to determine how long sessions vended using this profile are valid for.
                  See the Expiration section of the CreateSession API documentation (https://docs.aws.amazon.com/rolesanywhere/latest/userguide/authentication-create-session.html#credentials-object)page
                  for more details. In requests, if this value is not provided, the default
                  value will be 3600.
                format: int64
                type: integer
              enabled:
                description: Specifies whether the profile is enabled.
                type: boolean
              managedPolicyARNs:
                description: A list of managed policy ARNs that apply to the vended
                  session credentials.
                items:
                  type: string
                type: array
              name:
                description: |-
                  The name of the profile.

                  Regex Pattern: `^[ a-zA-Z0-9-_]*$`
                type: string
              requireInstanceProperties:
                description: |-
                  Specifies whether instance properties are required in temporary credential
                  requests with this profile.
                type: boolean
              roleARNs:
                description: |-
                  A list of IAM roles that this profile can assume in a temporary credential
                  request.
                items:
                  type: string
                type: array
              roleRefs:
                items:
                  description: "AWSResourceReferenceWrapper provides a wrapper around
                    *AWSResourceReference\ntype to provide more user friendly syntax
                    for references using 'from' field\nEx:\nAPIIDRef:\n\n\tfrom:\n\t
                    \ name: my-api"
                  properties:
                    from:
                      description: |-
                        AWSResourceReference provides all the values necessary to reference another
                        k8s resource for finding the identifier(Id/ARN/Name)
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                  type: object
                type: array
              sessionPolicy:
                description: |-
                  A session policy that applies to the trust boundary of the vended session
                  credentials.
                type: string
              tags:
                description: The tags to attach to the profile.
                items:
                  description: A label that consists of a key and value you define.
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            description: ProfileStatus defines the observed state of Profile
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  partition:
                    description: Partition is the AWS partition in which the resource
                      exists or will exist
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              attributeMappings:
                description: A mapping applied to the authenticating end-entity certificate.
                items:
                  description: A mapping applied to the authenticating end-entity
                    certificate.
                  properties:
                    certificateField:
                      type: string
                    mappingRules:
                      items:
                        description: A single mapping entry for each supported specifier
                          or sub-field.
                        properties:
                          specifier:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: The ISO-8601 timestamp when the profile was created.
                format: date-time
                type: string
              createdBy:
                description: The Amazon Web Services account that created the profile.
                type: string
              profileID:
                description: |-
                  The unique identifier of the profile.

                  Regex Pattern: `[a-f0-9]{8}-([a-z0-9]{4}-){3}[a-z0-9]{12}`
                type: string
              updatedAt:
                description: The ISO-8601 timestamp when the profile was last updated.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
